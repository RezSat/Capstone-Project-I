# SEO

## Purpose

Search engine optimization for the storefront.

## Implementation

- Server-side rendering for product and category pages
- Dynamic meta tags (title, description, Open Graph)
- Canonical URLs
- Semantic HTML structure
- `sitemap.xml` and `robots.txt`
- Structured data for products
- Image alt text
- Clean URL structure (`/products/:slug`, `/category/:group/:slug`)
