# Common Components Ownership

Use this folder for reusable components shared by storefront and dashboard.

- Keep components prop-driven and presentation-focused.
- Avoid embedding business logic.
- Reuse `src/components/ui/*` primitives where possible.
